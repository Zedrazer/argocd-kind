image:
  repository: bitnami/postgresql
  tag: latest

auth:
  postgresPassword: postgres
  username: pactbroker
  password: pactbroker
  database: pactbroker

primary:
  persistence:
    enabled: false
