auth:
  postgresPassword: postgres
  username: pactbroker
  password: pactbroker
  database: pactbroker

primary:
  persistence:
    enabled: false

image:
  registry: docker.io
  repository: postgres
  tag: "15"
